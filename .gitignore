# Claude Code のローカル設定
.claude/

# 手元の作業用データ（サイトには不要）
*.xlsx

# 更新スクリプト（サイトには不要。公開もしない）
*.ps1

# npm の作業ディレクトリ。テストは Node の標準機能だけで動くので
# 本来できないはずだが、うっかり npm install したときに公開されないようにする
# （公開対象から外す指定は _config.yml 側にもある）
node_modules/
